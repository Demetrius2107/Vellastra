name: add-test
description: 为指定类生成单元测试（JUnit 5 + Mockito），生成后自动编译验证
prompt: |
  为 {target_class} 生成单元测试。

  目标类路径: {target_path}
  测试类路径: {test_path}

  ## 测试要求

  ### 1. 测试框架
  - 使用 JUnit 5（@Test, @DisplayName, @ExtendWith(MockitoExtension.class)）
  - 使用 Mockito 模拟依赖（@Mock, @InjectMocks）
  - 使用 @BeforeEach 初始化

  ### 2. 测试覆盖范围
  - 正常路径（happy path）：至少 1 个测试
  - 异常路径（entity not found、参数错误）：至少 1 个测试
  - 边界条件（null 入参、空集合）：可选

  ### 3. 测试命名规范
  - 方法名: {methodName}_{scenario}_should{ExpectedResult}
  - 示例: getUserById_existing_shouldReturnVO, getUserById_notFound_shouldThrow

  ### 4. 断言
  - 使用 AssertJ 或 JUnit 原生断言
  - 验证返回值（assertEquals, assertNotNull, assertThrows）
  - 验证 Mock 交互（verify(mock).method(), verifyNoMoreInteractions）

  ### 5. 注意事项（避免 CI 失败）
  - void 方法（如 repository.save()）必须用 doAnswer() 而非 thenAnswer()
  - 模拟 void 方法用 doAnswer() / doNothing() / doThrow()
  - 模拟有返回值的方法用 when().thenReturn() / when().thenAnswer()

  ### 6. 生成后自动验证
  生成测试文件后，立即执行以下命令验证编译：

  ```bash
  # 编译测试代码，检测是否有编译错误
  mvn test-compile -pl {module} -q

  # 如果编译失败，按错误信息修复后重新验证
  # 直到 mvn test-compile 输出无错误
  ```

  ### 7. 代码示例
  ```java
  @ExtendWith(MockitoExtension.class)
  class UserServiceTest {

      @Mock
      private UserRepository userRepository;

      @InjectMocks
      private UserService userService;

      @Test
      @DisplayName("getUserById 存在时返回 UserVO")
      void getUserById_existing_shouldReturnVO() {
          User user = User.builder().id(1L).username("test").build();
          when(userRepository.findById(1L)).thenReturn(user);

          UserVO vo = userService.getUserById(1L);
          assertEquals("test", vo.getUsername());
      }

      @Test
      @DisplayName("save 为 void 方法时用 doAnswer 示例")
      void save_shouldUseDoAnswer() {
          doAnswer(invocation -> {
              User u = invocation.getArgument(0);
              u.setId(1L);
              return null;
          }).when(userRepository).save(any());
      }
  }
  ```